'use strict';

const Tools = require('./tools');
const PRNG = require('./prng');

class BattlePokemon {
                if (!ignoreImmunities && status.id && !(source && source.hasAbility('electrojection') && status.id in {'par': 1})) {
			                  // the game currently never ignores immunities
			                  if (!this.runStatusImmunity(status.id === 'par' : status.id)) {
				                        this.battle.debug('immune to status');
				                        if (sourceEffect && sourceEffect.status) this.battle.add('-immune', this, '[msg]');
				                        return false;
			                  }
	            	}
};
